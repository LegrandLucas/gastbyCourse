"use strict";

exports.__esModule = true;
exports.clientVisitedPageReducer = void 0;
// The develop server always wants these page components.
const defaults = new Set();
defaults.add(`component---cache-dev-404-page-js`);
defaults.add(`component---src-pages-404-js`);

const clientVisitedPageReducer = (state = new Set(defaults), action) => {
  switch (action.type) {
    case `DELETE_CACHE`:
      return new Set(defaults);

    case `CREATE_CLIENT_VISITED_PAGE`:
      {
        state.add(action.payload.componentChunkName);
        return state;
      }

    default:
      return state;
  }
};

exports.clientVisitedPageReducer = clientVisitedPageReducer;
//# sourceMappingURL=client-visited-page.js.map